const firstYearCompleted = true;
const yearsCompleted = 1;

yearsCompleted == firstYearCompleted
yearsCompleted === firstYearCompleted

// La diferenza sta nel fatto che nel primo caso non viene fatto il paragone tra i tipi di dati messi a confronto()
// mentre nel secondo sì, quindi i valori dovrebbero essere uguali sia come valore che come tipo di dato(dicassi strettamente uguali)